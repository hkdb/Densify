name: Build for Linux

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build-linux:
    name: Build for Linux
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Get version from tag
        id: get_version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=dev" >> $GITHUB_OUTPUT
          fi
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y desktop-file-utils
          sudo ./AppImage/bootstrap.sh
          python3 -m pip install --upgrade pip setuptools wheel
          pip3 install pycairo==1.25.1 PyGObject
      - name: Set up environment
        run: |
          cp .env.ci .env
      - name: Build package
        run: |
          python3 build.py
      - name: Create tar.gz archive
        run: |
          VERSION=${{ steps.get_version.outputs.VERSION }}
          mkdir -p "Densify-${VERSION}"
          cp densify "Densify-${VERSION}/"
          cp densify.desktop "Densify-${VERSION}/"
          cp *.png "Densify-${VERSION}/"
          cp __init__.py "Densify-${VERSION}/"
          cp requirements.txt "Densify-${VERSION}/"
          cp install.sh "Densify-${VERSION}/"
          cp AppImage/io._3df.osi.densify.metainfo.xml "Densify-${VERSION}/"
          cp LICENSE "Densify-${VERSION}/" 2>/dev/null || true
          cp README.md "Densify-${VERSION}/" 2>/dev/null || true
          tar -czvf "Densify-${VERSION}.tar.gz" "Densify-${VERSION}"
          mv "Densify-${VERSION}.tar.gz" dist/
      - name: Store the distribution packages
        uses: actions/upload-artifact@v4
        with:
          name: linux-dist
          path: dist/
